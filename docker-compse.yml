version: '3.1'
 node:
    build: .
    command: npm run start

    ports:
      - "5000:5000"
    depends_on:
      - postgres

 postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: 123456



