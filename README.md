## Task 3: node.js project with Typescript

### Commands:

- `npm start` &mdash; start code compiled by tsc
- `npm run build` &mdash; compile start with tsc
- `npm run dev` &mdash; start code ts in development mode
